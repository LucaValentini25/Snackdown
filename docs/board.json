{
  "project": "Snackdown",
  "updated": "2026-08-25",
  "currentEpic": "loose-ends",

  "epics": [
    {
      "id": "netcode-core",
      "title": "Netcode core — predicted character",
      "phase": "1",
      "status": "done",
      "goal": "A character that responds instantly on the owning client, is decided by the server, and self-corrects when the two disagree. Everything else in the project is scaffolding around this.",
      "tasks": [
        { "id": "nc-1", "title": "Fixed 30 Hz tick wired to NetworkTickSystem", "status": "done", "test": "EditMode — tick ordering" },
        { "id": "nc-2", "title": "InputCommand sampled on the owner and sent to the server", "status": "done", "test": "EditMode — input sanitation" },
        { "id": "nc-3", "title": "Shared replayable Simulate() used by both sides", "status": "done", "test": "EditMode — 40 ticks replayed twice are bit-identical", "notes": "Covers the arithmetic; the terrain-cast path runs with GroundMask = 0 in fixtures" },
        { "id": "nc-4", "title": "Client prediction with a (tick, input, state) ring", "status": "done", "test": "EditMode — PredictionBufferTests" },
        { "id": "nc-5", "title": "Server snapshots carrying lastProcessedInputTick", "status": "done", "test": "—" },
        { "id": "nc-6", "title": "Reconciliation — rewind to snapshot, replay pending inputs", "status": "done", "test": "none — lives inside a MonoBehaviour and is unreachable from EditMode", "notes": "90 lines, 9 branches, 0% coverage. The project's headline mechanism" },
        { "id": "nc-7", "title": "Snapshot interpolation for remote players", "status": "done", "test": "EditMode — SnapshotInterpolatorTests" },
        { "id": "nc-8", "title": "Debug overlay: predicted vs authoritative, RTT, corrections", "status": "done", "test": "—", "notes": "Costs ~97% of host managed allocation, which is why pl-5 later took it out of release builds" },
        { "id": "nc-9", "title": "Validated under simulated latency and packet loss", "status": "done", "test": "manual — 150 ms / 20% loss, median error 0.302 units", "notes": "Written up in docs/05" }
      ]
    },
    {
      "id": "connection-layer",
      "title": "Connection layer — LAN and Relay behind one flow",
      "phase": "2",
      "status": "done",
      "goal": "The same join flow whether the peer is on the LAN or across the internet, with the door closed to anything unchecked.",
      "tasks": [
        { "id": "cl-1", "title": "IConnectionProvider — async throughout, failures as return values", "status": "done", "test": "—" },
        { "id": "cl-2", "title": "DirectConnectionProvider — host and join by IP", "status": "done", "test": "manual — two peers on a LAN" },
        { "id": "cl-3", "title": "RelayConnectionProvider — join by six-character code", "status": "done", "test": "manual — a real hosted session", "notes": "Sessions API; project linked with Relay and Lobby enabled" },
        { "id": "cl-4", "title": "Connection approval with payload and version check", "status": "done", "test": "EditMode — nickname sanitation, ConnectionApprovalTests" },
        { "id": "cl-5", "title": "SessionRoster — replicated names, skins, ready state", "status": "done", "test": "—", "notes": "Rewritten by ps-2: the NetworkList and PlayerSlot are gone, the three fields live on PlayerSession, and the roster is an index over them in Gameplay/Player" },
        { "id": "cl-6", "title": "Menu and lobby in UI Toolkit, wired to the abstraction", "status": "done", "test": "—" },
        { "id": "cl-7", "title": "Character picker", "status": "dropped", "test": "—", "notes": "Transport half shipped, picker never did. Moved to the wardrobe epic where it belongs" }
      ]
    },
    {
      "id": "gameplay-core",
      "title": "Gameplay core — the rules, server-authoritative",
      "phase": "3",
      "status": "done",
      "goal": "The whole pitched loop running end to end, with every rule decided by the server against server-owned positions.",
      "tasks": [
        { "id": "gc-1", "title": "Life timer draining server-side, replicated ~1 Hz", "status": "done", "test": "—", "notes": "1 write/s by configuration, plus an immediate publish on pickup and on death" },
        { "id": "gc-2", "title": "Fruit spawner with a rarity table, networked pickup", "status": "done", "test": "EditMode — FruitTableTests", "notes": "8 fruit, 35% to 1%, worth 3s to 20s" },
        { "id": "gc-3", "title": "Head-bounce detection and stun", "status": "done", "test": "EditMode — StunTests", "notes": "Stun lives in PlayerState so reconciliation replays through it" },
        { "id": "gc-4", "title": "Players solid to each other, predicted client-side", "status": "done", "test": "EditMode — PeerCollisionTests", "notes": "Resolves against interpolated peer positions — see known problems" },
        { "id": "gc-5", "title": "Death to spectator, win conditions, end screen", "status": "done", "test": "EditMode — ArenaBoundsTests", "pr": 19, "notes": "Player hidden rather than despawned — the workaround the player-session epic removes" }
      ]
    },
    {
      "id": "hud",
      "title": "In-match HUD — life bars and round clock",
      "phase": "4",
      "status": "done",
      "goal": "Make the core mechanic visible. A draining life timer nobody can see makes the phase complete on paper and undemoable in practice.",
      "tasks": [
        { "id": "hud-1", "title": "Round clock", "status": "done", "test": "—", "pr": 23 },
        { "id": "hud-2", "title": "Life bars, built both ways — floating and along the bottom", "status": "done", "test": "—", "pr": 23, "notes": "F3 swaps them at runtime so the better one can be picked by looking" },
        { "id": "hud-3", "title": "Nameplates sized in metres, not by transform scale", "status": "done", "test": "—", "pr": 23, "notes": "Three consecutive sizing bugs; the rule they came from is written down in docs/01" },
        { "id": "hud-4", "title": "Pick one layout and delete the other", "status": "dropped", "test": "—", "notes": "Superseded by D-006 — it becomes a player preference instead of a choice to make" }
      ]
    },
    {
      "id": "player-session",
      "title": "Separate player identity from avatar",
      "phase": "4",
      "status": "done",
      "goal": "One networked object per connection that owns identity, life and stats, and spawns a disposable avatar per round. Unblocks late join, real despawn on death, the wardrobe and host-configured matches — none of which can be built cleanly on top of NGO's default one-prefab-per-connection model.",
      "tasks": [
        { "id": "ps-0", "title": "Stand up the networked test harness", "status": "done", "test": "PlayMode — HandshakeTests: the client is synchronized, the host sees it, it sees the host, and two clients see each other", "notes": "The testables entry this task was written around turned out to be the wrong route and was not added — see D-011. NetworkedFixture is ours: 4 tests, no manifest change" },
        { "id": "ps-1", "title": "PlayerSession exists alongside the avatar, unread", "status": "done", "test": "PlayMode — PlayerSessionTests: a joining client gets a session of its own, both peers read the sanitized name, the host's own session carries the name it chose, and a leaving client takes its session with it", "notes": "Cost two things the task did not anticipate: NetworkSimulation had to become a prefab to be spawnable in a test (D-013), and the session reads its own name out of approval rather than being handed it, because the object that spawns it lives in the assembly this one depends on" },
        { "id": "ps-2", "title": "Roster becomes an index over live sessions", "status": "done", "test": "PlayMode — SessionRosterTests: a client joining a running session sees all four players with their names, skins and ready flags; every peer orders the list the same way; a client cannot ready up a player it does not own", "notes": "NetworkList and PlayerSlot are deleted as planned, and the roster went with them — it cannot name PlayerSession from Snackdown.Connection, so it moved to Gameplay/Player (D-014). Two things the task did not anticipate: the pre-spawn write ps-1 promised turns out to log a warning on every join and was not done (D-015), and the harness had a real flake — every session bound the same UDP port, so a session now takes one of its own" },
        { "id": "ps-3", "title": "Life, fruit count and stats move onto the session", "status": "done", "test": "PlayMode — FruitPickupTests: a fruit the client walks into adds life and a count on the server and both reach the client, the host is not credited for it, and a fruit nobody is standing on collects nothing and spawns silently", "notes": "PlayerLife moved as a component onto the PlayerSession prefab rather than being folded into the class (D-016), so the referee, the HUD and the spectator camera did not change at all. Stats stayed at one number — fruit, cumulative for the connection — narrowing D-003, which had said per-round. The four places that reached life through the avatar now resolve owner to session, which is what forced PlayerSession.Of to take the peer it should look in (D-017). Two things found on the way: D-015 was reasoning from a wrong premise and is corrected, and FruitSpawner had been logging a warning per fruit for the same reason" },
        { "id": "ps-4", "title": "Flip the spawn — PlayerPrefab becomes the session", "status": "done", "test": "PlayMode — AvatarLifecycleTests: GetPlayerNetworkObject returns the session and connecting alone grants no body; running out despawns the character on both peers while the session keeps its name and the life it ended on; the next round hands back a different character at the point it was asked for, with the life refilled", "notes": "Both workarounds are gone as planned. The teleport went further than the note expected: with a character created at its spawn point instead of moved there, PlayerSnapshot lost its IsTeleport flag and the packet dropped from 8+42N to 8+41N (D-018). PredictedPlayer lost the life reference, IsSolid and the hide-on-death branch, and HeadBounce lost its alive check. The life reset moved from returning to the lobby onto starting a round, which closes a hole where a rematch from the end screen would have begun on the previous round's leftovers" },
        { "id": "ps-5", "title": "Late join arrives as a spectator", "status": "done", "test": "manual, reproduced by Luca through the real UI with Multiplayer Play Mode: a second player joining a match already under way gets no character and a free camera, and when the host drained out the end screen read Nobody was left standing rather than handing the win to the arrival. Also checked in-process against a running sandbox: the server holds the joiner not alive with no character on any peer. PlayMode — AvatarLifecycleTests covers the other side of the boundary, that joining in the lobby still arrives in the round", "notes": "The board's own test is not reachable in the harness: it needs phase Playing, and NetworkedFixture runs with EnableSceneManagement off because both peers share one editor scene list. Verified against a real join instead, with the improvised second peer created in-process — see D-019, which records this as a deliberate exception to D-007 rather than a gap. No new replicated state: the match sits a late arrival out through the same ServerEndRound that running out uses, so the camera, the referee and the next round all needed no changes" },
        { "id": "ps-6", "title": "Host can kick, with a reason the kicked client sees", "status": "done", "test": "PlayMode — KickTests: a client asking the server to remove another client is ignored and nobody leaves; the host's request disconnects the player, who reads SessionRoster.KickReason on the way out, and the roster drops them without anyone editing it; a request to kick the host itself is refused. Checked by cutting the sender comparison, which fails the first test with \"A client kicked another client\"", "notes": "The reason is a fixed constant, not typed by the host: free text would be a second player-supplied string crossing the wire needing everything the nickname needs. Nothing removes the player from the roster by hand — NGO despawns what a departing client owned and the roster is rebuilt from what is spawned. Came with a fix the task did not ask for: the lobby now watches for losing its own connection, so a client whose host quit stops sitting in a dead lobby screen listing players who are gone" },
        { "id": "ps-7", "title": "Replicated match settings and difficulty profiles", "status": "done", "test": "PlayMode — MatchSettingsTests: the host lowers the starting life to 30, the client sees 30 and a round reset starts a player at 30; a client asking to change the rules or load a preset is ignored; numbers out of range are clamped rather than refused, with zero round seconds surviving because that is the sentinel for no clock; a preset replaces every number at once on the client. Checked by cutting the sender comparison, which fails the client test with \"A client changed the rules everyone plays by\"", "notes": "Nothing Simulate() reads is exposed, as D-005 said. What D-005 had wrong is why this replicates at all — two of the five numbers are read by clients rather than only applied by the server, so the ADR's why is corrected in place. The lobby got a rules panel: preset dropdown plus a field per number, greyed for clients and for the host once a match is under way, since the server refuses a change then anyway — confirmed working in Play by Luca, which is the check the editor could not run here. Three presets authored — Relaxed, Standard (the asset that already existed), Brutal" }
      ]
    },
    {
      "id": "wardrobe",
      "title": "Wardrobe — unique, changeable skins",
      "phase": "4",
      "status": "done",
      "goal": "First free skin on arrival, changeable in the lobby, never two players wearing the same one. Nearly free once the skin lives on the session.",
      "tasks": [
        { "id": "wr-0", "title": "Return to lobby comes back to the lobby, not to the menu", "status": "done", "test": "manual — with a session live, the Lobby scene was unloaded and reloaded the way a match does it, and what came back was the lobby screen with the join code still on it rather than the host-or-join screen. Not reachable from a test: it is scene lifetime and UI Toolkit, and the harness has neither", "notes": "Reported by Luca after playing. The Lobby scene is unloaded at Countdown and reloaded when the phase returns, so what comes back is a fresh MainMenuController whose OnEnable ends in ShowMenu() without asking whether a session is live. The connection is never lost; only the screen is. Two parts, and the second is why this is not a one-liner: OnEnable has to choose the screen from the live session, and the join code has to survive — _rawJoinTarget and the IConnectionProvider that produced it are fields of the controller and die with the scene, so even the right screen comes back with an empty code row and a Copy button with nothing to copy. Fixed by moving the connection out: a SessionConnection in the bootstrap scene, which never unloads, owns the provider, the approval and the join target, and the menu asks it instead of building its own. That also closed something nobody had noticed — the lazy rebuild meant a second ConnectionApproval was constructed on the next lobby load while the static still pointed at the first. Rejected: leaving the lobby scene loaded and only hiding it, which is smaller but contradicts the one-owner rule for that scene and keeps a UIDocument alive through every match" },
        { "id": "wr-1", "title": "Approval assigns the first free skin instead of always 0", "status": "done", "test": "PlayMode — SkinAssignmentTests: four players all asking for skin 0 are admitted with four different ones inside the catalog; two peers agree on a third player's skin; a skin freed by somebody leaving is given to the next arrival. Checked by making the request be honoured regardless, which fails with \"Four players asked for skin 0 and were given 0, 0, 0, 0\"", "notes": "Nothing sets the index in the payload yet, so every arrival asks for 0 — the old behaviour was not a bug that could be pointed at, it was every request being honoured exactly as written. Assigned in approval because that is where the taken set is known and where the answer is recorded; the index the session publishes is the admitted one. More players than skins clamps and allows duplicates rather than turning somebody away over something cosmetic. Freeing works because approval already forgets a client on disconnect. CharacterCount is still the hardcoded 4 — wr-2" },
        { "id": "wr-2", "title": "Wire CharacterCount from CharacterCatalog.Count", "status": "done", "test": "PlayMode — SkinAssignmentTests: with a catalog of five, a client asking for skin 4 is admitted as 4 rather than clamped to 3. The wiring itself checked in Play: growing the catalog in memory and rebuilding takes approval.CharacterCount from 4 to 5, which no test can reach because the harness has no bootstrap scene", "notes": "Settled by D-020: SessionConnection moved to Snackdown.Gameplay so it can name the catalog, which is the same move D-014 made for the roster. No new components, no new types, no new edges between assemblies" },
        { "id": "wr-3", "title": "Wardrobe UI in the lobby, taken skins shown as taken", "status": "done", "test": "PlayMode — SkinAssignmentTests: a client asking for a skin somebody is wearing is refused and keeps its own; a client changing into a free one is accepted and every peer sees it; a skin let go of is given to the next arrival. Checked by removing the refusal, which fails with \"A client took a skin somebody else was wearing\". The row of buttons itself is for Luca to look at", "notes": "The request is an owner-checked Rpc on PlayerSession and is refused outright when the skin is taken, rather than quietly given the nearest free one — a player who clicked a costume and silently got another would think the wardrobe was broken. Refused once a match is under way too. The change is reported back to ConnectionApproval, whose table hands skins to arrivals: left describing what everyone walked in wearing, it would keep an abandoned skin reserved for nobody. The lobby draws taken skins disabled rather than hiding them, so the row does not change length while somebody is reaching for one" },
        { "id": "wr-4", "title": "A random default nickname, and the chosen one remembered", "status": "done", "test": "EditMode — NicknamePreferenceTests: with nothing remembered a name is still offered and it is not SystemInfo.deviceName; a name gone into a session with is offered again, trimmed; a blank one is not worth remembering; forgetting goes back to random. Every test saves the real PlayerPrefs value and puts it back, so running the suite cannot cost the person running it their name", "notes": "Was only the PlayerPrefs half; Luca asked for the default too, so both are here. Today MainMenuController.DefaultNickname returns SystemInfo.deviceName, which is the computer's name — it reaches every other player through the roster, so this is a small privacy leak as much as it is a dull default. Replaced by a list of names picked at random, overridden by whatever is saved. The first PlayerPrefs in the project — a name is a preference of the person at the keyboard, not state of the match. What is stored is what the player typed and not what they were admitted as: approval numbers duplicates, so remembering \"Ana (2)\" would make them \"Ana (2) (2)\" the next time somebody else was already Ana, drifting a bracket further every evening. The name list is in code rather than an asset, unlike the arenas and the fruit — it is flavour on a field the player is expected to overwrite, not something tuned while balancing. Also the first time the EditMode suite references Snackdown.UI" },
        { "id": "wr-5", "title": "HUD layout preference, host default plus local override", "status": "done", "test": "—", "notes": "D-006" },
        { "id": "wr-6", "title": "Two players with the same nickname are told apart", "status": "done", "test": "PlayMode — NicknameTests: three players all asking for Ana are admitted as Ana, Ana (2) and Ana (3); every peer sees the same names; a number freed by somebody leaving goes to the next arrival; a name at the cap keeps its suffix and stays inside MaxNicknameLength. Checked by removing the numbering, which fails all four", "notes": "Reported by Luca. In ConnectionApproval next to the sanitising, because the name a session carries is the admitted one — a list that disambiguated on its own would show something no other list agreed with. Numbering starts at two: the player already using the name keeps it plain, since renaming them because somebody else arrived would move a name out from under a player who did nothing. The suffix wins over the name when they do not both fit, so the cap the FixedString32Bytes relies on still holds" }
      ]
    },
    {
      "id": "verification",
      "title": "Verification — make the netcode claims checkable",
      "phase": "5",
      "status": "done",
      "goal": "Close the gap where no test in the repository can fail because of a networking bug, and no byte has ever been counted.",
      "tasks": [
        { "id": "vf-1", "title": "Integration test — a dropped snapshot leaves no permanent offset", "status": "done", "test": "EditMode — ReconcilerTests.ADroppedSnapshot_LeavesNoPermanentOffset: a client wrong from the start loses one snapshot entirely, and the next one puts it exactly where the server would have it at the newest predicted tick", "notes": "Written as a unit test rather than the integration test the title asked for, which vf-2 is what made possible. It drives the real PlayerMotor against a real MovementConfig, so the replay is the same arithmetic the server ran — a fake step would have proved the plumbing and nothing about convergence. The first draft of it was wrong and passed for the wrong reason: it had client and server agreeing by construction, so every snapshot was an agreement and nothing was ever replayed" },
        { "id": "vf-2", "title": "Extract Reconcile so it can be tested without a MonoBehaviour", "status": "done", "test": "EditMode — ReconcilerTests: replay converges on the server's answer after a forced desync, and each of the eight decisions has a test — first snapshot, out of order, the same one twice, inside tolerance, past the buffer, no memory of the tick, prediction off, and after a desync", "notes": "A Reconciler in Snackdown.Netcode holding the two fields that existed only for reconciling — the newest acknowledged tick and whether a baseline exists — and returning a ReconcileResult the character applies. Position, smoothing and telemetry stayed with the MonoBehaviour, which is the half that needs a transform. This is what docs/01 prescribed for exactly this case" },
        { "id": "vf-3", "title": "Seeded 100k-roll fruit distribution test", "status": "done", "test": "EditMode — FruitDistributionTests: a hundred thousand seeded rolls against the shipped table land within a point of the weights; the commonest and rarest are the 35% and 1% the docs name; every fruit comes up at least once; the ends of the roll and out-of-range values stay inside the table", "notes": "Against the asset the game ships rather than a table built in the test — what is worth protecting is that this table produces the spread the docs describe. Seeded, with the seed written down: an unseeded test of this shape fails once a month for nobody's reason and teaches everyone to re-run it until it goes green" },
        { "id": "vf-4", "title": "Measure bandwidth with the profiler already installed", "status": "done", "test": "manual — two profiler captures taken by Luca during real two-peer runs, integrated over the frames of a running match by reading the Network category counters out of the saved .data files. Written into docs/05", "notes": "3 657 B/s out of the host at two players, of which 3 001 is snapshots and 21 is every NetworkVariable in the game. Outgoing is identical at 2% and 20% loss, which is the expected answer and the reason to trust the number: snapshots leave once per tick whatever happens to them afterwards. Incoming falls by 0.74x against the 0.8x that 20% loss predicts, so the two halves confirm each other. The serializer-derived figure was short by about 11 bytes a snapshot — NGO's RPC framing, which reading the payload could not see" }
      ]
    },
    {
      "id": "polish",
      "title": "Polish and release",
      "phase": "5",
      "status": "todo",
      "goal": "Make the finished work visible to someone who was not here while it was built.",
      "tasks": [
        { "id": "pl-1", "title": "Decide how peer contact is predicted", "status": "done", "test": "EditMode — PeerExtrapolationTests covers the arithmetic behind the new option: no gap, a tick already described, a few ticks at a known velocity, a stationary rival that must not drift, and the cap — that it bites at the right tick and not the one before, and that it applies on both axes. The comparison itself is manual and is written up in docs/05", "notes": "Luca chose to build both and measure rather than settle it by argument. PredictedPlayer.PeerContact switches between the interpolated positions the client is drawing and the last authoritative snapshot carried forward at its velocity, F5 flips it, the setting is written into every exported run, and flipping restarts the recording so the two never average together. The extrapolation is capped at a quarter second — a rival whose snapshots stopped arriving keeps the velocity they vanished with, and unbounded that puts them metres inside the scenery. IPredictedPeer.ApplySnapshot grew the frame tick, because the time it already carried answers a different question: the time lines remote characters up against the render clock, the tick says how far a state has to be carried forward. The default is unchanged, so every number in docs/05 still describes what the game does" },
        { "id": "pl-2", "title": "Spectator camera follows a player and switches targets", "status": "done", "test": "EditMode — SpectatorTargetRingTests: fourteen cases over the choice of who to watch, including the one the type exists for — the player being watched dies and the camera lands on the next one along rather than back at the top of the roster — plus wrapping at both ends, one player left, everybody dead, and a list that grows for the next round", "notes": "Following replaced free-look, which asked a player who had just died to go and find the fight first. A tap left or right switches, edge-triggered so holding the stick moves one player rather than scrolling at frame rate, and the roster order is the one the bottom strip already shows. The choice lives in a SpectatorTargetRing the camera owns but does not contain — the same split as Reconciler, for the same reason. Candidates are built from the characters rather than the sessions, because a session has no transform and on a client does not even know which object its avatar is, and filtered by NetworkManager so the two-peer harness does not offer each peer the other one's players. The bottom strip outlines whoever is being watched: Arena01 is 26x9 against a 24.9x14 view, so the camera barely moves and switching would otherwise have no visible effect at all" },
        { "id": "pl-3", "title": "A second arena", "status": "done", "test": "manual — host, pick Arena 02 in the lobby, start: the client sees the choice before it starts and both peers land in the warm arena. The catalog and the build list are checked in the editor: two entries, five scenes, Sandbox still off", "notes": "Arena01 duplicated with the red and blue channels of every colour swapped, which is what Luca asked for and is better than it sounds — swapping two channels preserves luminance, so the contrast between ground, walls and background is unchanged and nothing became harder to read. Identical geometry: a palette, not a second level design. The task was filed as authoring only and was not: nothing could reach the second arena, because the lobby hardcoded ServerStartMatch(0). So the rules panel gained an arena dropdown, host-only like the numbers and hidden entirely with a single arena, writing the same _arenaIndex the match start already reads. That replicates the choice for as long as the lobby is open, which is the point — a player readying up can see where they are agreeing to play" },
        { "id": "pl-4", "title": "Public lobby browser", "status": "done", "test": "EditMode — JoinTargetTests covers the layer the UI is rendered from: a listing keeps a readable name when the service gives none, full is only full when there is no room, an empty browse is a success with nothing in it rather than a failure, a failed one still hands back a list a caller can iterate, and a cancelled one says nothing. The screen itself was checked by hand against a real Relay session", "notes": "Reached through IConnectionProvider, which grew CanBrowse and BrowseAsync and nothing more — the LAN provider answers no, because there is no directory behind a socket, and the whole section is hidden rather than left empty. What comes back is a SessionListing of four values, not the service's ISessionInfo, which would have put Unity.Services in the UI assembly. Sessions are created public and named after the host. Nothing polls: the service rate-limits and a timer would spend that quota on a screen nobody is looking at, so Refresh is a button" },
        { "id": "pl-5", "title": "Keep debug tooling out of the player build", "status": "done", "test": "— not unit-testable: the flag it turns on is the configuration the tests run in. Verified by reading what the guards cover and by the editor behaving unchanged — F1–F4, the ghost and the CSV export all still work, and the 97 EditMode tests still pass", "notes": "One flag, DebugTools.Enabled, true in the editor and in development builds. It gates the overlay, the red authoritative ghost and the run recorder — the three things the audit blamed for ~97% of host managed allocation. The overlay destroys itself in Awake rather than returning early from OnGUI, so Unity stops calling it at all. Tried as a const first and reverted: a constant folds, and every guard then compiles to unreachable code and a warning in the editor. NetworkConfigReport was left in on purpose — one log line per connection attempt is what makes a NetworkConfig mismatch diagnosable in a build somebody else is running" },
        { "id": "pl-6", "title": "A runnable build, main brought up to date, a tag", "status": "in-progress", "test": "the build itself: Windows x64, release configuration, 102 MB, 0 errors and 0 warnings, four scenes in it and seven Snackdown assemblies. Running it and playing a match across two machines is Luca's step", "notes": "The build half is done and is the half that was never true before — docs/04 recorded that no player build had ever been produced, which is why it refused to cut a 1.0.0. Being a release build is also what makes pl-5 real: DEVELOPMENT_BUILD is undefined, so DebugTools.Enabled is false and the overlay, the ghost and the recorder are not in it. Builds/ stays gitignored; a 102 MB directory belongs on a GitHub Release, not in a repository. What is left needs Luca: merging into main and cutting the tag" },
        { "id": "pl-7", "title": "Asking for a code is a step of joining, not of the front screen", "status": "done", "test": "manual — the front screen offers a name, Host and Join and no code field; Join opens a screen with the list, the code field and Back. EditMode: JoinTargetTests fixes the part that could break silently, which is a typed code and a clicked listing being told apart", "notes": "Reported by Luca: the field sits on the menu next to Host, where it means nothing — a host never types a code. Choosing Join should be what asks for it, on a screen of its own with the field and a Join button. Luca asked for it to travel with pl-4, and it should: a browser and a typed code are two ways through the same step, and building that step twice is how they end up disagreeing" }
      ]
    },
    {
      "id": "loose-ends",
      "title": "Loose ends",
      "phase": "5",
      "status": "todo",
      "goal": "Clear the small open risks that have been sitting on the board since the epics that created them, before anything larger is started on top of them. Grew one task Luca asked for while it was open.",
      "tasks": [
        { "id": "le-1", "title": "Fruit does not outlive the arena it was spawned in", "status": "done", "test": "manual in the sandbox, both routes out of an arena: with six fruit standing, returning to the lobby leaves zero, and a start refused mid-flight from Ended leaves zero. The root cause was confirmed the same way rather than assumed — a live fruit reports its scene as Sandbox, the active one, while the arena is Arena01, so the unload was never going to take it", "notes": "Wired through a new MatchDirector.ArenaUnloading event rather than the director despawning fruit itself. Today Fruits depends on Match and not the reverse, and a director reaching into the fruit would have pointed that both ways for one call. Raised inside UnloadCurrentScene because both routes out of an arena pass through it, so hanging it off either caller would have left the other leaking. The spawner resolves its director once and holds it, instead of reading MatchDirector.Current the way its Update does: subscribing and unsubscribing have to reach the same object, and a process-wide static is not that under the harness" },
        { "id": "le-2", "title": "Check whether snapshots still go out while the session sits in the lobby", "status": "done", "test": "manual in the sandbox: SnapshotsSent froze at 705 with zero characters registered while the phase sat in Lobby, and rose again the moment a match started. The risk was retired on that reading, not on the code review that predicted it", "notes": "Mostly a risk to retire rather than work to do — ps-4 made the round hand out a fresh body, so no character exists in the lobby, nothing is registered with the tick loop and BroadcastSnapshot returns before building a frame. But looking for it found a real one next door: a match start refused mid-flight puts the phase back to Lobby without despawning the bodies, so the session sat in the lobby with characters spawned into no scene and snapshots still going out for them. The despawn came out into ServerDespawnBodies and both routes call it now" },
        { "id": "le-4", "title": "A scene showing every sprite at the size its import settings produce", "status": "done", "test": "— it is an authoring aid and nothing runs in it. Checked that all 113 sprites resolved: 120 renderers, none with an empty sprite", "notes": "Asked for by Luca to fix sprites that are set up wrong. Built as plain SpriteRenderers at scale 1 rather than anything clever, because the whole value is that what is drawn is exactly what the importer produces. A row per source folder, and a strip of 1x1 unit squares with the character beside them so size can be judged against something. It immediately paid for itself: the pack ships three different pixels-per-unit and the terrain is at 100, so a 16x16 tile is 0.16 units against a one-unit character — a sixth of the size the art was drawn at. Listed in Build Settings with its checkbox off, like Sandbox, so Unity stops adding it back" },
        { "id": "le-3", "title": "Sandbox holds an instance of the NetworkSimulation prefab, not a copy of it", "status": "done", "test": "the sandbox still starts a match on Play with a clean console, and the instance now carries exactly one override — the rules asset. Checked first that nothing else in the scene held a serialized reference into the object being replaced", "notes": "The drift the risk predicted had already happened and was found by diffing the copy against the prefab before touching anything: two differences, and only one of them was meant. _rules pointing at SandboxMatchConfig is the deliberate override and was kept; _difficulties being null was ps-7 landing on Bootstrap for free and being forgotten here, and it fixed itself by coming from the prefab. That is the argument for D-013 arriving on time rather than as a prediction" }
      ]
    }
  ],

  "decisions": [
    {
      "id": "D-021",
      "date": "2026-08-25",
      "epic": "polish",
      "title": "Every hosted game is public",
      "choice": "Sessions are created with IsPrivate false and named after the host, so all of them appear in the browser. The join code still works and is still how you reach a specific game.",
      "why": "Being listed is not a second kind of session, it is the same one with a door somebody can find. A browser that only shows the games whose hosts remembered to tick a box is a browser that is usually empty, which is the failure mode the feature exists to avoid. The code alone is already what a game nobody can find looks like.",
      "rejected": "A public/private toggle on the front screen. It is one checkbox and can be added the day somebody wants it, but adding it now would mean shipping a browser whose most likely state is empty."
    },
    {
      "id": "D-001",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "Separate player identity from avatar, and do it first",
      "choice": "One PlayerSession NetworkObject per connection, owning identity and life; a disposable avatar spawned per round.",
      "why": "Four wanted features — late join, real despawn on death, the wardrobe and returning to the lobby — are each awkward on their own under NGO's default model, and each stops being a special case once identity and avatar have separate lifetimes. Building them first means building them twice.",
      "rejected": "Doing the wardrobe and match config first for faster visible progress; keeping NGO's model and patching late join and death as special cases."
    },
    {
      "id": "D-002",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "The roster becomes an index, not a store",
      "choice": "PlayerRoster keeps no data of its own — it is an ordered view over the live sessions, the same pattern as PlayerLife.All.",
      "why": "Nickname and skin already exist in three places, which works only because the flow is one-directional and once-only. The wardrobe, per-round avatars and late join all break that condition, and a value with three owners fails by showing each peer something different — no crash, no exception, and rare enough to be blamed on the network.",
      "rejected": "Letting the roster and the sessions both hold identity; keeping the NetworkList as the source and having sessions read it."
    },
    {
      "id": "D-003",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "Life, fruit count and match stats live on the session",
      "choice": "The session tracks life, fruit eaten and per-round stats, all server-validated. The avatar is physical only; the HUD reads the session.",
      "why": "If the avatar has to survive death to keep the life value, it stays the owner of identity and the refactor achieves nothing. Moving life up also removes a branch from PredictedPlayer.IsSolid: a dead player has no avatar, so peer-collision prediction never considers it, rather than checking a flag.",
      "rejected": "Leaving PlayerLife on the avatar and continuing to hide the character instead of despawning it."
    },
    {
      "id": "D-004",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "Keep NGO's auto-spawn and repoint it",
      "choice": "NetworkManager.PlayerPrefab becomes the PlayerSession prefab. The avatar becomes an ordinary network prefab the session spawns with SpawnWithOwnership.",
      "why": "Disabling AutoSpawnPlayerPrefabClientSide and hand-rolling the spawn would mean rewriting ConnectionApproval's admission path and losing GetPlayerNetworkObject. Repointing keeps both, and makes NGO's own clientId-to-object lookup return exactly the object worth reaching.",
      "rejected": "Turning auto-spawn off and spawning both objects manually from the approval callback."
    },
    {
      "id": "D-005",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "Match settings replicate as a struct; nothing Simulate() reads is configurable",
      "choice": "A MatchSettings struct in a server-written NetworkVariable, seeded from difficulty presets and overridable field by field. MovementConfig stays out of it entirely.",
      "why": "CORRECTED in ps-7 on one point. What holds: everything Simulate() reads is executed identically on both sides of the wire, and a divergence there produces a trembling character whose symptom points at reconciliation, which is not where the bug is — so movement stays out. What was wrong: this said the tunable numbers are read by the server alone and reach clients as an already-applied result, so a mismatch would cost nothing. Two of them are read by clients. DrainPerSecond is what a client counts its own life down with between the server's once-a-second updates, and MaxLife is the denominator of every life bar drawn; a host moving either against clients holding the old asset makes every other screen disagree. The struct had to replicate, which is what the choice above says, but not for the reason first written down. Everything Simulate() reads is executed identically on both sides of the wire, and a divergence there produces a trembling character whose symptom points at reconciliation, which is not where the bug is.",
      "rejected": "One flat replicated config covering movement too; closed difficulty profiles with no per-field override."
    },
    {
      "id": "D-006",
      "date": "2026-08-23",
      "epic": "wardrobe",
      "title": "HUD layout is a player preference with a host-set default",
      "choice": "The host picks the room default; each player can override it locally, remembered in PlayerPrefs.",
      "why": "It is the only setting that changes nothing about the match, only how one screen looks. Replicating it means a player whose taste differs from the host's plays all night with the layout they did not want, for no rule-level reason.",
      "rejected": "Host-controlled and replicated for everyone; picking one layout and deleting the other."
    },
    {
      "id": "D-007",
      "date": "2026-08-23",
      "title": "Tests ship with the feature, not as a phase",
      "choice": "Every task carries the test that verifies it, written in the same PR. The networked harness is stood up first so this is possible at all.",
      "why": "A verification phase at the end is a phase that gets cut. The cost of not having this is already measured: a join-breaking regression survived six merged PRs over 47 hours, and the commit that fixed it says so in its own body.",
      "rejected": "Writing the integration tests after the refactor, against the new architecture; leaving them in Phase 5 as originally planned."
    },
    {
      "id": "D-008",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "Naming — PlayerSession and PlayerRoster",
      "choice": "PlayerSession for the per-connection object, PlayerRoster for the index over them.",
      "why": "Controller already means a character controller in Unity, and the object being named is a connection with an identity attached, which is what a session is. Roster keeps the name the UI layer already uses.",
      "rejected": "PlayerController, PlayerHub, NetworkPlayer."
    },
    {
      "id": "D-009",
      "date": "2026-08-23",
      "epic": "polish",
      "title": "Public lobby browser is deferred to the end",
      "choice": "Join by code only until the player lifecycle and the wardrobe are finished.",
      "why": "It demonstrates no netcode that the connection layer does not already demonstrate, and it adds a screen and a lifecycle of its own. It is worth having for how it looks in a demo video, which is a reason to do it late rather than never.",
      "rejected": "Building it now as the product-facing front of the project."
    },
    {
      "id": "D-010",
      "date": "2026-08-23",
      "title": "The board is generated, never hand-kept",
      "choice": "docs/board.json is the only file edited. A script renders docs/06-board.md for readers and docs/local/board.html for working.",
      "why": "A board is touched on every task, and two hand-maintained copies of a thing edited that often disagree within a week — the same failure the audit found across the docs, where the reasoning stayed excellent and the facts drifted. One source removes the possibility rather than requiring discipline.",
      "rejected": "Hand-writing the HTML alongside the markdown; keeping the board only as an HTML page, which the repository's own rules forbid as a deliverable."
    },
    {
      "id": "D-011",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "The networked harness is ours, not the one NGO ships",
      "choice": "A NetworkedFixture in a PlayMode assembly that creates a NetworkManager and a UnityTransport per peer on loopback. manifest.json is untouched.",
      "why": "NGO's NetcodeIntegrationTest is only compiled by listing the package under testables, and in 2.11 it lives in Unity.Netcode.Runtime.Tests together with the package's own 672 tests — all of which would then appear in this project's Test Runner, so the suite the project is judged on stops being legible in its own window. What that harness does for the ordinary case is one NetworkManager and one transport per peer, which is roughly a hundred lines written in this project's terms, against an API that is public and stable rather than one that was already renamed once in 2.0.",
      "rejected": "Adding the testables entry the ps-0 task was originally written around, and inheriting from NetcodeIntegrationTest."
    },
    {
      "id": "D-012",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "Networked tests load the real prefabs, not ones built in code",
      "choice": "PlayerSessionTests loads Player, PlayerSession and NetworkSimulation from the asset database. The test class is wrapped in UNITY_EDITOR; the assembly stays a Play mode one.",
      "why": "Fabricating a prefab at runtime needs NetworkObject.GlobalObjectIdHash, which is internal — NGO can only do it in its own tests because its assembly is granted access. A runtime-built object keeps hash 0, and this task needs two of them, so registering the second is refused with a duplicate-hash error. Loading the real assets sidesteps that and buys the half a hand-built prefab could never cover: that the object which actually ships is wired up. An editor-only assembly was tried and reverted — the Test Runner reclassifies it as Edit mode, where NGO never initialises its message table and every test fails on the first message sent.",
      "rejected": "Writing the internal hash by reflection; moving the prefabs into a Resources folder that every build would then carry."
    },
    {
      "id": "D-013",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "A networked scene object becomes a prefab when it needs to be tested",
      "choice": "NetworkSimulation — the one object carrying the tick loop, the roster, the director and the referee — was saved as a prefab and the scene now holds an instance of it.",
      "why": "A scene object cannot be spawned by a test: the harness runs both peers in one editor process, so they share the scene and therefore the object, where a real session gives each peer its own copy through scene synchronization. Without this the only way to test a join was to bypass the roster and spawn sessions by hand, which tests the class and not the thing the task asks for. Saving it as a prefab also means the scene and the test exercise the same object rather than two configurations that drift.",
      "rejected": "Splitting SessionRoster onto a GameObject of its own; leaving ps-1 without a test of the real join path."
    },
    {
      "id": "D-014",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "SessionRoster moved to Gameplay and stopped replicating anything",
      "choice": "The roster is an index over the live PlayerSession objects, ordered by owner id, living in Snackdown.Gameplay.Player. NetworkList<PlayerSlot> and the PlayerSlot struct are deleted; name, skin and ready state are NetworkVariables on the session, and the ready Rpc moved there with them.",
      "why": "The roster held a second copy of three facts the session already owns, kept in step by hand on the server — and a second copy is a copy that can disagree, which is the failure this whole epic exists to remove. Indexing means naming PlayerSession, and Snackdown.Connection cannot: Gameplay depends on it, not the other way round. So the file moved to the assembly that was always describing it, the same answer that broke the Netcode/Gameplay cycle in Phase 1. Nothing was lost on the wire — a late joiner still arrives to a full list, because NGO synchronizes the spawned session objects for the same reason it used to send the whole NetworkList. The roster keeps three jobs: spawning one session per connection, ordering the list so every peer draws the lobby identically, and raising one Changed event.",
      "rejected": "Keeping the roster in Connection behind an IPlayerSession interface — an abstraction bought purely to keep a file in the wrong folder. Deleting the roster outright and letting each view read PlayerSession.All, which leaves nobody owning the order or the change event, and hands every reader a static that is shared between peers under the test harness."
    },
    {
      "id": "D-015",
      "date": "2026-08-23",
      "epic": "player-session",
      "title": "The session still adopts its identity after spawn, not before",
      "choice": "PlayerSession keeps reading its name and skin out of ConnectionApproval in OnNetworkSpawn. Both are inside the spawn message every client receives. CORRECTED in ps-3 — the original entry claimed they arrived as deltas after it, and that was wrong.",
      "why": "The decision is right and the original reasoning behind it was not, so both are recorded. What is true: writing a NetworkVariable before Spawn logs \"NetworkVariable is written to, but doesn't know its NetworkBehaviour yet\" — NGO does not attach a variable to its behaviour until the object spawns, and the flag that silences it is internal to the package. Verified in ps-3 by making a fruit do it and watching the test catch it. What was wrong: this was written up as a trade against two extra deltas. There are no deltas. NetworkObject.SpawnInternal runs the server's own spawn — and therefore OnNetworkSpawn — before calling SendSpawnCallForObject, which serializes each variable's current value, so anything written there is already in the initial state clients receive. Writing before Spawn buys nothing at all and costs a warning per join. The ps-1 note that promised this as a follow-up was wrong on the same point.",
      "rejected": "Reaching NetworkVariableBase.IgnoreInitializeWarning by reflection. Spawning the session and having the roster write the values straight after, which is a real delta with an extra hop."
    },
    {
      "id": "D-016",
      "date": "2026-08-24",
      "epic": "player-session",
      "title": "PlayerLife moved to the session as a component, not as fields on it",
      "choice": "The PlayerLife component was taken off the Player prefab and put on the PlayerSession prefab unchanged. PlayerSession exposes it as Life and adds one number of its own, the fruit count. The class was not merged into PlayerSession.",
      "why": "Moving the component keeps one file to one type: the session is identity, PlayerLife is the rules of the life clock, and they are now two components on one object instead of one class doing both. It also cost almost nothing to the rest of the project — the referee, the bottom-strip HUD, the spectator camera and the director all read PlayerLife.All, which is unchanged, so none of them were touched. Folding the fields into PlayerSession would have meant rewriting all four to read PlayerSession.All and leaving a large class that mixes a player's name with the drain rate. What did have to change is the four places that reached life through the avatar — the fruit, the head bounce, the predicted character and the nameplate — and those had to change under either option.",
      "rejected": "Merging life into PlayerSession and deleting PlayerLife. Leaving PlayerLife on the avatar and having the session hold a reference to it, which is the arrangement this epic exists to undo."
    },
    {
      "id": "D-017",
      "date": "2026-08-24",
      "epic": "player-session",
      "title": "Looking a player up takes the peer to look in",
      "choice": "PlayerSession.Of(NetworkManager peer, ulong clientId) rather than Of(clientId). The single-argument version was removed rather than kept beside it.",
      "why": "Game code now crosses from one networked object to another to find a player: a fruit sees a character, resolves its owner, and writes to that owner's session. PlayerSession.All is a static, and the PlayMode harness runs a host and its clients in one process, so it holds several peers' copies of the same player and the first match is not reliably the caller's. Reading the wrong one is a wrong answer; writing to it is a permission error NGO logs and nothing acts on, which means a fruit that is silently never banked. Keeping a one-argument overload beside it would leave the footgun in the API and a remark explaining not to use it. In a shipped build every copy belongs to the only peer there is and the argument costs nothing.",
      "rejected": "Documenting the hazard and keeping the short overload. Having each caller filter PlayerSession.All itself, which is the same three lines copied into four files."
    },
    {
      "id": "D-020",
      "date": "2026-08-24",
      "epic": "wardrobe",
      "title": "The thing that composes a session lives above the pieces it composes",
      "choice": "SessionConnection moved from Snackdown.Connection to Snackdown.Gameplay. It builds the provider and the approval as before, and now also holds the CharacterCatalog and tells approval how many skins exist.",
      "why": "Approval decides which skin an arrival is given and has to know how many there are. The catalog is content and lives in Snackdown.Gameplay, which already depends on Snackdown.Connection — so approval cannot name it, and neither could SessionConnection while it sat beside approval. Moving the composer up is the same answer D-014 reached for the roster and the one that broke the Netcode/Gameplay cycle in Phase 1: the file was in the wrong folder. It costs nothing in the graph — no new components, no new types, no new references between assemblies — and a composition root naming everything it composes is what a composition root is for.",
      "rejected": "A component in Gameplay that pushes the count into the connection layer, which is two components on one object to move one integer and an ordering to be careful about. Moving CharacterCatalog into Snackdown.Connection, which would put sprites and display names in the layer that is supposed to know only about connecting."
    },
    {
      "id": "D-018",
      "date": "2026-08-24",
      "epic": "player-session",
      "title": "The teleport flag leaves the wire with the reposition it announced",
      "choice": "PredictedPlayer.ServerTeleport, its announce counter and PlayerSnapshot.IsTeleport are deleted. A round spawns a character at its spawn point rather than moving one that already exists. The snapshot is 8+41N bytes instead of 8+42N — 172 rather than 176 with four players.",
      "why": "The flag existed for exactly one situation: the server repositioning a character the owner was already predicting, which on the wire is indistinguishable from prediction having gone badly wrong. It was worth a byte per player per tick because the alternative was a spawn placement counted as a 3.8-unit correction, poisoning the one statistic the netcode layer is judged by. That situation no longer occurs — the character is created where it belongs, so every peer starts out agreeing about its position instead of being corrected into agreement. Keeping a public server API and a wire field for a case the game can no longer reach is exactly the leftover CLAUDE.md forbids, and a bool that is always false is worse than no bool.",
      "rejected": "Keeping the mechanism as an unused capability and recording it as debt, which leaves dead public API and a field nobody can explain. Repurposing it for a mid-round respawn, which contradicts this task's own test — death despawns the character."
    },
    {
      "id": "D-019",
      "date": "2026-08-24",
      "epic": "player-session",
      "title": "The match layer ships verified by hand, and the reason is written down",
      "choice": "ps-5 landed with a manual verification against a real join instead of the PlayMode test its task asked for. The half that is reachable — joining in the lobby still arrives in the round — is automated.",
      "why": "Everything the match decides is gated on the phase, and every route to a phase past Lobby runs through a networked scene load. NetworkedFixture disables scene management because its two peers share one editor process and therefore one scene list, which is the machinery D-011 deliberately did not import from NGO. Enabling it is not a small change and its cost is unknown; extracting the phase machine into something EditMode can drive, which docs/01 recommends for cases like this, would test the transitions and still not test a join. So the honest options were a manual verification recorded in full or a task left open, and the feature was worth more than the checkbox. This is the second task in a row the same wall has blocked — the fix for the discarded scene-event status shipped the same way — so it is a decision now rather than a habit.",
      "rejected": "Opening the harness to scene management first, which is the part of NGO's own test rig D-011 rejected and whose cost is unknown. Extracting the phase machine, which buys transition tests and not this one. Skipping ps-5 for ps-6, which is testable, and leaving the epic with a hole in the middle."
    }
  ],

  "risks": [
    {
      "title": "The lead to the authoritative position was unattributed",
      "impact": "Closed by measurement. Over Relay: 400ms of lead, a 3.0 unit gap, queue at 2-3. On loopback, same match, same build: under 1 unit and a queue of 1-2. The 317ms that was unattributed is the relay hop, and prediction is doing exactly its job. MaxQueueDepth was exonerated on the way — it is 8 and the queue never approaches it, so lowering it would have cost jitter tolerance for nothing. The floor left on loopback is one or two ticks of queue plus NGO's client lead, with nothing to reclaim. Recorded in docs/05 along with why a rendezvous costs what it does: every message crosses the internet twice, which is the price of nobody opening a port.",
      "severity": "low",
      "status": "closed"
    },
    {
      "title": "Neither peer-contact source has been measured",
      "impact": "pl-1 built both and left the choice open, which is what Luca asked for. Nothing has run the comparison in docs/05 yet, so the project still cannot say which is better — only that it can now find out. The default is unchanged, so nothing measured so far is invalidated. The measurement is two people deliberately colliding under 20% loss and cannot be automated: the harness cannot reach a match at all (D-019).",
      "severity": "low",
      "status": "open"
    },
    {
      "title": "Whether the reconciler extraction moved the error level is still open",
      "impact": "Re-measured on 2026-08-25 and the answer is not clean. Scenario B's median error went from 0.302 u to 0.407 u, but median measured RTT went from 367 ms to 433 ms in the same runs and median replayed ticks from 11 to 13 — a client that predicted further ahead disagrees by more, and RTT is not something the profile sets directly. What did not move is the shape: corrections worth exactly one tick of movement were 27% before and 29% after, which is the claim a broken replay would have destroyed. A run at matched RTT would settle it.",
      "severity": "low",
      "status": "open"
    },
    {
      "title": "Sandbox held a copy of NetworkSimulation, not an instance of the prefab",
      "impact": "Closed by le-3. Sandbox holds an instance now, with one override — the rules asset. The prediction had already come true and nobody had noticed: diffing the copy against the prefab found _difficulties null, which is ps-7 landing on Bootstrap for free and being forgotten here. Replacing the object with an instance; not done here because it is a scene change with nothing else in this epic depending on it.",
      "severity": "low",
      "status": "open"
    },
    {
      "title": "Nothing past phase Lobby can be reached in the test harness",
      "impact": "Every match decision is gated on the phase, and every route past Lobby runs through a networked scene load — which NetworkedFixture disables, because its peers share one editor process and one scene list. So the countdown, the round clock, the referee's verdicts, the arena's spawn points and anything conditioned on a match being under way are unreachable from a test and ship verified by hand. Twice so far: the discarded scene-event status, and ps-5. See D-019.",
      "severity": "high",
      "status": "open"
    },
    {
      "title": "Almost no test in the repository can fail because of a networking bug",
      "impact": "The handshake is now covered by PlayMode tests over a real transport, which is the layer the regression that survived six merged PRs broke. Approval, the session, the roster, the fruit pickup and the avatar lifecycle followed. Reconciliation is still verified by nothing.",
      "severity": "high",
      "status": "open"
    },
    {
      "title": "Ambient statics are shared by peers under the test harness",
      "impact": "Eight one-value statics — the Current pointers, PlayerLife.All, PlayerSession.All, ActivePlayers — are per-process, which is right for a shipped game and wrong for a harness that runs several peers in one editor. Tests must read NetworkManager.SpawnManager instead, and anything whose behaviour depends on one of these cannot be told apart between peers. Narrowed by ps-3: the two lookups that game code uses to cross between objects, PlayerSession.Of and SessionRoster.Of, now filter by peer (D-017). The registries themselves and the Current pointers are unchanged.",
      "severity": "medium",
      "status": "open"
    },
    {
      "title": "Reconcile has zero test coverage",
      "impact": "Closed by vf-2. The decision half moved into a Reconciler that needs no scene and has a test per branch. What is still only visible in a session is the applying half — moving the transform and telling the smoother — and the behaviour under real latency, which docs/05 measures by hand.",
      "severity": "high",
      "status": "closed"
    },
    {
      "title": "No byte of bandwidth has ever been measured",
      "impact": "Closed by vf-4. Measured from the profiler at two players and written into docs/05, and the derived figure turned out short by the RPC framing rather than wrong. What is still arithmetic is the four-player case, which is now the open item.",
      "severity": "medium",
      "status": "closed"
    },
    {
      "title": "main is 55 commits behind and no build has ever been produced",
      "impact": "A reviewer following the repository link lands on the Phase 0 scaffold. Managed stripping and scene-by-name resolution are unexercised.",
      "severity": "medium",
      "status": "open"
    },
    {
      "title": "The debug overlay ships enabled and dominates host allocation",
      "impact": "Closed by pl-5. The overlay, the authoritative ghost and the run recorder are gated on DebugTools.Enabled and are gone from a release build; the overlay destroys itself in Awake so its OnGUI is never called. Development builds keep all three deliberately — that is where they explain the project. Nothing has re-measured allocation in an actual release build, because pl-6 has not produced one yet.",
      "severity": "medium",
      "status": "closed"
    },
    {
      "title": "Peer contact is predicted against interpolated positions",
      "impact": "Roughly 0.47 s stale at the measured RTT, so close contact between two moving players reliably corrects. Fixing it invalidates the Phase 1 measurements.",
      "severity": "medium",
      "status": "open"
    },
    {
      "title": "Snapshots broadcast at 30 Hz while the session sits in the lobby",
      "impact": "Retired by le-2 as already fixed, and on a measurement rather than a reading: SnapshotsSent froze while the phase sat in Lobby. ps-4 made the round hand out a fresh body, so no character exists in the lobby and nothing is registered with the tick loop. Looking for it did find a real bug next door — a refused match start left the bodies spawned — which is fixed.",
      "severity": "low",
      "status": "closed"
    },
    {
      "title": "FruitSpawner.ServerDespawnAll has no call sites",
      "impact": "Closed by le-1. The spawner clears itself on MatchDirector.ArenaUnloading, which fires inside the unload on both routes out of an arena. The cause was sharper than this line recorded: fruit is instantiated without a parent, so it belongs to the active scene while the spawner belongs to the arena — the unload was never going to take it.",
      "severity": "low",
      "status": "closed"
    }
  ],

  "log": [
    {
      "date": "2026-08-26",
      "entry": "Closed the loose-ends epic, and the interesting part is that two of the three were not what the board said they were. The fruit leak had a sharper cause than recorded — fruit is instantiated without a parent so it belongs to the active scene, not the arena, which a live check confirmed by asking a fruit which scene it was in. The lobby-snapshot risk was already closed by ps-4 and retired on a reading of the counter rather than on the code review that predicted it — but looking for it found a real bug next door: a match start refused mid-flight went back to the lobby without despawning the bodies, leaving characters in no scene and snapshots still going out. And the Sandbox drift D-013 warned about had already happened, silently, when ps-7 added the difficulty catalog. All three were verified in the sandbox, which is the only route this project has to a running match — the harness still cannot reach one."
    },
    {
      "date": "2026-08-26",
      "entry": "Closed the question about the gap to the ghost, and it closed the way the project would want: by measurement rather than by tuning. Loopback, same match and same build, puts the gap under a unit and the queue at 1-2, against 3.0 units and 2-3 over Relay. The 317ms nobody could account for is the relay hop — a rendezvous means every message crosses the internet twice, which is the price of nobody having to open a port. Nothing was changed to get there: the input queue was exonerated, the client lead has nothing to reclaim, and the separation Luca was looking at turned out to be the round trip drawn to scale. It is also the whole argument of the project, measured end to end: over a 400ms link the player feels nothing and the entire visible cost is a ghost three units back."
    },
    {
      "date": "2026-08-26",
      "entry": "Measured the input queue rather than guessing at it, and the guess was wrong in the useful direction. MaxQueueDepth is 8 and the queue sits at 2-3, so the branch that drains an extra command has almost certainly never run and lowering the cap would have bought nothing while costing jitter tolerance. A depth of two or three is what a well-tuned client lead looks like. It accounts for 83ms of a 400ms lead; the rest is transit and NGO's own lead and is still unattributed. Worth recording that the gap Luca was looking at is exactly the round trip and nothing more: 400ms at 7 u/s predicts 2.8 units and 3.0 were measured, so there is no unexplained separation to find. The loopback control is what is left."
    },
    {
      "date": "2026-08-26",
      "entry": "Both of Luca's reports turned out to be the same kind of thing: a control that lied about its state. The arena picker was working the whole time — what was broken is that the field is 130px with an empty UI Toolkit label inside eating a third of it, so 'Arena 01' and 'Arena 02' both rendered as 'Arena 0'. The field had room for every character except the one that told them apart, and the screenshots show the tick in the open menu sitting on the right entry while the closed field read the same either way. The two dropdowns also came out flat grey against white number fields beside them, which reads as disabled on a control the host is meant to use. Both are USS: the internal label is hidden, the column went to 150px, the popup input is painted like the fields around it, long names ellipsize, and disabled is now visibly disabled. And the delay was F1. The exported run says it outright — zero corrections in 66.7 seconds, against 140 in 122 seconds the day before on the same 430ms Relay connection. Nothing was wrong with the netcode; prediction was off, which is exactly what that key is for demonstrating. The run header now records whether prediction was on, because a file with no corrections and nothing saying why reads as the netcode being broken."
    },
    {
      "date": "2026-08-26",
      "entry": "Luca reported the arena picker not taking — selecting Arena 02 and the field going back to the first. Could not reproduce: driven end to end in Play mode the chain works, dropdown to RPC to replicated index to Arena02 actually loading, and it survives repeated roster refreshes. The likeliest explanation is the pitfall docs/05 already records, that Unity defers compilation while a session is live, so a change made during play has not been compiled. The field was hardened anyway on the part that was genuinely weak: it is driven by index rather than by matching the display name, since what replicates is a position in the catalog and a string comparison in the middle of that would resolve two arenas of the same name to the first; and the choices are refilled when they and the catalog disagree, which covers AttachDirector running against a director that has been found but not yet spawned. Also checked the delay Luca reported on the joining peer: the NetworkSimulator in Bootstrap has an all-zero preset and no preset asset, so nothing in the repository is adding impairment."
    },
    {
      "date": "2026-08-25",
      "entry": "Produced the first player build the project has ever had: Windows x64, release, 102 MB, no errors and no warnings, in nine minutes. docs/04 had refused to cut a 1.0.0 precisely because no build existed, and that sentence is now false. It also settles pl-5 in the only way that counts — a release build has DEVELOPMENT_BUILD undefined, so the overlay that was 97% of host managed allocation is not in the thing anybody would download. The rest of pl-6 is not work, it is permission: main and the tag are Luca's."
    },
    {
      "date": "2026-08-25",
      "entry": "Closed pl-3, and it was not the authoring-only task the board had it down as. The scene duplicated and recoloured in a minute; what took the work is that nothing could reach it, because the lobby called ServerStartMatch(0) with the zero written in. So the arena became a replicated choice like the numbers are: a dropdown in the rules panel, host-only, hidden with a single arena, writing the same variable the match start already reads. The useful consequence is that everyone can see which arena is coming while they decide whether to ready up. The recolour is a channel swap rather than new colours picked by eye — it keeps every luminance identical, so the contrast the arena was authored with survives."
    },
    {
      "date": "2026-08-25",
      "entry": "Closed pl-1 by not deciding it, which is what it needed. The roadmap had held the question open since Phase 1 on the grounds that choosing well means feeling the difference — so both ways of predicting peer contact are built now and F5 picks between them, with the setting written into every exported run and the recording restarted on each flip so two settings can never average into one number. What is left is a measurement, and it is honestly a manual one: it is two players deliberately colliding under heavy loss, and the harness cannot reach a match at all. The extrapolation came out into its own static and got a test per awkward case, which is the fourth time this project has found that the way to test something was to move it out of the MonoBehaviour it was living in."
    },
    {
      "date": "2026-08-25",
      "entry": "Closed pl-7 and pl-4 together, which is how Luca asked for them and how they wanted to be built: a browser and a typed code are two ways through the same step, and building that step twice is how the two end up disagreeing. The front screen is a name and two buttons now; Join opens a screen with the list, the field and Back. The interesting part was keeping the menu ignorant of the service — IConnectionProvider grew exactly two members, the listing is four values rather than the SDK's own type, and the LAN provider simply answers that it has nothing to browse. One thing had to be named rather than guessed: a join code and a listing id are both strings pointing at the same session and the service has a separate call for each, so the request carries which it is. Guessing from the shape would have worked until a code looked like an id."
    },
    {
      "date": "2026-08-25",
      "entry": "Started the polish epic on one branch, as Luca asked — a PR per task had become the slowest part of the work. pl-5 took the overlay, the authoritative ghost and the run recorder out of release builds behind one flag, closing the highest-severity performance risk on the board: the audit had measured the overlay at ~97% of the host's managed allocation and it was in every build. It was written with a const first and reverted, because a constant folds and every guard then compiles to unreachable code and a warning in the editor. pl-2 turned the spectator camera from a free-look into something that follows a survivor and switches on a tap; the choice of who to watch came out into a plain class, which is the third time in this project that making something testable meant moving it out of a MonoBehaviour."
    },
    {
      "date": "2026-08-23",
      "entry": "Read the repository end to end after time away: 64 scripts, six design documents and a ten-domain audit. Conclusion recorded — the netcode is solid and measured; everything unfinished sits in the lobby, identity and configuration layer."
    },
    {
      "date": "2026-08-23",
      "entry": "Designed the player-session refactor and took ten decisions on it. Confirmed by inspection that the auto-spawn does not need disabling, that every host-configurable parameter is server-read only, and that the EditMode suite is untouched by the change."
    },
    {
      "date": "2026-08-23",
      "entry": "Stood up this board: one JSON source, two generated views, and a rule in CLAUDE.md that closing a task, an epic or a session updates it."
    },
    {
      "date": "2026-08-23",
      "entry": "Closed ps-0 with a harness of our own after opening the testables route and finding it worse than the note in the task assumed (D-011). Four handshake tests now run over a real transport. Two things learned while writing it and worth having recorded: a NetworkManager added at runtime has no NetworkConfig at all, and Shutdown() only raises a flag — the socket closes on the next network update, so a fixture that destroys the peer in the same frame leaves the port bound and breaks the next test rather than its own."
    },
    {
      "date": "2026-08-23",
      "entry": "Closed ps-1. Three things were learned by trying: a prefab cannot be built at runtime without an internal NGO field (D-012), a networked scene object cannot be spawned by a test at all (D-013), and the ambient statics documented in docs/01 as safe because a peer is a process stop being safe the moment a harness runs two peers in one. The last one is recorded as a risk and the architecture doc now says so rather than claiming the opposite. Each new test was checked by breaking the code it covers: removing the session spawn fails exactly the four session tests and none of the handshake ones."
    },
    {
      "date": "2026-08-23",
      "entry": "Closed ps-2. The roster stopped replicating a player list and became an index over the sessions, which deleted PlayerSlot and moved the roster out of Snackdown.Connection (D-014). The pre-spawn write ps-1 left as a follow-up turned out to be a console warning per join and was rejected rather than inherited (D-015). Adding the tests surfaced a real flake in the harness: every session bound the same UDP port, and roughly one run in three failed to bind it in whichever test ran next, so a session now takes a port of its own."
    },
    {
      "date": "2026-08-24",
      "entry": "Closed ps-3. PlayerLife moved onto the session prefab and the fruit now credits a player rather than a body. Both new tests were checked by breaking what they cover: matching the pickup on any NetworkObject instead of on a character makes a fruit collect itself, and writing the fruit's kind before Spawn trips the log assertion. That second one settled a claim I had got wrong in ps-2 — a NetworkVariable written in the server's OnNetworkSpawn is already inside the spawn message, so D-015's talk of deltas was reasoning from a wrong premise and is corrected in place. The port fix from ps-2 also turned out to be half a fix: the leftover socket is not always from this run, so the harness now probes a port before binding it. Related and not fixed: the editor process is holding UDP 7777 from a Play session that ended, which is why the sandbox cannot host without an editor restart — Enter Play Mode Options has domain reload disabled, so a leaked socket outlives the session that opened it."
    },
    {
      "date": "2026-08-24",
      "entry": "Closed ps-4, and with it both workarounds the epic was written to remove: a player who is out is despawned rather than hidden, and a round spawns a character at its spawn point rather than moving one that was already standing somewhere else. The second went further than the task note expected — with nothing left to reposition, PlayerSnapshot could drop its teleport flag, which is the first time this project has taken something off the wire (D-018). Also closed a hole nobody had hit yet: the life reset lived on the way back to the lobby, a path a rematch started from the end screen never travels, so it moved onto the call that hands out a body. The death-to-despawn wiring was checked by cutting it — two of the four new tests then fail on a timeout rather than an assertion, which is what a despawn that never arrives looks like."
    },
    {
      "date": "2026-08-24",
      "entry": "Fixed the thing ps-4 only observed. MatchDirector discarded the SceneEventProgressStatus that NGO's LoadScene and UnloadScene return, so a load refused because another scene event was still in flight did nothing at all — no log, no exception, and a phase left in Loading waiting on reports for a scene nobody had asked for. The load is now checked and puts the phase back to Lobby with the reason in the console; the unload is reported but not acted on, since nothing downstream waits for it. Reproduced in the sandbox before and after: same two calls in one frame, phase Loading forever versus phase Lobby and \"Netcode refused to load Arena01: SceneEventInProgress\"."
    },
    {
      "date": "2026-08-24",
      "entry": "Closed ps-5. A player who arrives while a round is being played is sat out of it by the match, through the same call running out uses — no new replicated state, and the spectator camera, the referee and the next round all worked unchanged as a result. Verified against a real join into a running sandbox: not alive, no character on any peer, and ending the only player who had actually been playing gave NoWinner instead of handing the match to the arrival. The console was not clean during that check and the noise is worth naming — scene validation failures, handle mismatches and deferred-spawn timeouts, all from the improvised second peer sharing this process's scenes, none from Snackdown. A single-peer run after it was clean. The wall this ran into is now recorded as a risk and a decision (D-019) rather than being worked around a third time in silence."
    },
    {
      "date": "2026-08-24",
      "entry": "Luca reproduced ps-5 through the real UI with two Multiplayer Play Mode players: joined a match already under way, got no character and a free camera, and when the host drained out the end screen read Nobody was left standing. That is the assertion worth having — the spectator half is visible and would have looked right even with the bug, while the referee counting a latecomer among the living only shows up as the match picking the wrong winner. The board's test for ps-5 now records his run rather than mine."
    },
    {
      "date": "2026-08-24",
      "entry": "Closed ps-6, which is the first task in three that the harness could test end to end — kicking needs no phase past Lobby. Three tests, and the one that matters was checked by cutting the server's sender comparison: it fails with \"A client kicked another client\", so what passes it is the check and not the lobby hiding the button. Writing it turned up a gap nobody had filed: nothing was watching for this machine losing its connection, so a client whose host quit sat in a lobby screen listing players who were gone, with a Ready button that reached nobody. Being kicked needed that watch anyway, so both are fixed by the same handler. Also learned the hard way that git operations on ProjectSettings with the editor live break Unity's atomic save — it failed to write EditorSettings.asset and cost Luca a restart. Nothing was lost; the habit of reverting EditorBuildSettings mid-session is what caused it and has stopped."
    },
    {
      "date": "2026-08-24",
      "entry": "Closed ps-7 and with it the player-session epic. The rules a session plays by now ride a server-written struct instead of an asset each peer reads off its own disk, the host picks a preset or edits any number in the lobby, and the server clamps whatever arrives. Reading the code to write it turned up that D-005 was wrong about why this was needed — it said the tunable numbers are server-read only, and two of them are read by every client every frame. The decision it reached was right anyway; the reasoning is corrected in place, the third ADR in this epic to need that. The Play mode check of the lobby panel could not run here — the editor was holding UDP 7777 from an earlier session, the pitfall docs/05 records — and Luca confirmed the panel working in Play instead."
    },
    {
      "date": "2026-08-24",
      "entry": "Luca played a session and reported four things, now on the board rather than in a chat. The crucial one is wr-0: Return to lobby lands on the host-or-join screen and the whole lobby has to be set up again. It is not a regression from this epic — the Lobby scene is unloaded at Countdown and what comes back is a fresh MainMenuController that shows the menu without asking whether a session is live, which it has done since the menu was written. The other three: the default nickname is the computer's name and should be random plus remembered (folded into wr-4, which only covered the remembering), duplicate nicknames need a number in parentheses (wr-6, in approval where the name is already sanitised), and the code field belongs behind the Join button rather than on the front screen (pl-7, to travel with the lobby browser)."
    },
    {
      "date": "2026-08-24",
      "entry": "Closed wr-0, the one Luca called crucial. The menu was building and holding the connection itself — provider, approval and join target — in a scene that is unloaded every time a match runs, so returning from one produced a fresh controller that showed the host-or-join screen while the session was still running underneath it. A SessionConnection in the bootstrap scene owns all three now and the menu asks. It also closed a second-order bug nobody had reported: the provider was rebuilt lazily on the next lobby load, constructing a second ConnectionApproval beside the one the static still pointed at. Verified in Play by unloading and reloading the lobby against a live session — what came back was the lobby, with the join code still on it. No automated test: this is scene lifetime and UI Toolkit, and the harness has neither."
    },
    {
      "date": "2026-08-24",
      "entry": "Closed wr-1. Four players in a session are four different characters now, without anybody having chosen one — nothing sets the skin index in the payload yet, so every arrival asked for zero and every arrival was given it. Writing the test that a freed skin goes to the next arrival turned up a hole in the harness rather than in the game: JoinAnotherClient waited for every client ever started to be synchronized, including the one the test had just disconnected on purpose, so the join failed on a timeout that blamed the arrival. It now counts the clients still listening. wr-2, wiring the count to the catalog, stayed out: the catalog is in the assembly that depends on this one, so it is an assembly decision and not a wiring job."
    },
    {
      "date": "2026-08-24",
      "entry": "Closed wr-2 by moving SessionConnection up into Snackdown.Gameplay (D-020), which is the third time this project has answered a layering problem by admitting a file was in the wrong folder — the simulation types in Phase 1, the roster in ps-2, this now. A fifth skin is reachable: the count comes from the catalog rather than from a constant. The part no test can reach is the wiring itself, since the harness has no bootstrap scene, so it was checked in Play by growing the catalog in memory and watching approval follow it from 4 to 5."
    },
    {
      "date": "2026-08-24",
      "entry": "Closed wr-6, the second of Luca's four. Duplicate names are numbered at admission, beside the sanitising, because the name a session publishes is the admitted one. The tests caught a bug of their own before they caught anything else: NUnit builds one instance per fixture and runs every test in it, so the field holding what each peer asks to be called leaked from the cap test into three others and failed them against a name they never asked for. Reset per test now, and worth remembering — every PlayMode fixture here has state that would do the same."
    },
    {
      "date": "2026-08-25",
      "entry": "Closed wr-4, the third of Luca's four. The menu offered SystemInfo.deviceName — the computer's name, which reaches every other player through the roster, so it was a small leak as much as a dull default. It offers a random one now and remembers whatever the player actually went into a session with. First PlayerPrefs in the project, and the tests around it save and restore the real stored value: a suite that costs the person running it a setting is a suite they stop running. It is also the first time EditMode references the UI assembly, which is what let this be a unit test instead of something to look at."
    },
    {
      "date": "2026-08-25",
      "entry": "Closed wr-3 and wr-5, and with them the wardrobe epic. Changing skin is an owner-checked Rpc the server refuses outright when the costume is taken, and it tells approval about the change — that table is what hands skins to arrivals, so left alone it would keep an abandoned skin reserved for nobody. For wr-5, LifeBarStyle became a NetworkBehaviour on the session's own object: the host's default is replicated and each machine's own choice is not, which is D-006 exactly. It is the only replicated value in the project that no rule reads, and that is the argument for why the override stays local. Two things worth knowing: the fixture-state trap from wr-6 was waiting in the skin tests as well and was fixed before it bit, and the Sandbox scene turned out to hold a loose copy of NetworkSimulation rather than an instance of the prefab, so it had to be given the component by hand — recorded as a risk."
    },
    {
      "date": "2026-08-25",
      "entry": "Started the verification epic and closed three of its four. The reconciler came out of the MonoBehaviour it was trapped in, which is what docs/01 had been prescribing for this case since Phase 1, and the mechanism the whole project is pitched on went from zero coverage to a test per branch. vf-1 came free with it — the dropped-snapshot case is a unit test now rather than the integration test the task imagined. Its first draft passed for the wrong reason and is worth recording: it had the client and the server agreeing by construction, so every snapshot was an agreement, nothing was ever replayed, and the assertion compared a state at tick 7 against a position at tick 12. vf-3 was quick because Roll already took its randomness as an argument. What is left is vf-4, measuring bandwidth, which is a different kind of work — and a new risk: the latency figures in docs/05 were measured against the old reconciler and nobody has re-run them."
    },
    {
      "date": "2026-08-25",
      "entry": "Closed vf-4 and with it the verification epic, on a two-peer session Luca ran and I drove the instrumentation for. Bandwidth is measured rather than derived for the first time: 3 657 B/s out of the host at two players, and the figure the docs had been carrying was short by about eleven bytes a snapshot — NGO's RPC framing, which reading the serializers could not see. The measurement checks itself: outgoing is identical at 2% and 20% loss, which is what per-tick snapshots should do, and incoming falls by exactly the factor the loss predicts. The re-measured latency is less tidy and is recorded as such — scenario B's median error rose from 0.302 to 0.407 units, but so did the round trip it was measured at, and the distribution's shape is unchanged. Saying the extraction is proven harmless would be reading more into two unmatched runs than they carry."
    }
  ]
}
